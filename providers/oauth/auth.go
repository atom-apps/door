package oauth

// swagger:enum RegisterMethod
// ENUM(username, email, phone, password, captcha)
type RegisterField string

// swagger:enum SignInMethod
// ENUM(code,password)
type SignInMethod string
